#!/bin/sh /etc/rc.common

START=29
USE_PROCD=1

NAME=hw-management
PROG=/usr/bin/$NAME.sh

start_service() {
	procd_open_instance
	procd_set_param command bash -c "$PROG start"
	procd_close_instance
}

stop_service() {
    bash -c "$PROG stop"
}
